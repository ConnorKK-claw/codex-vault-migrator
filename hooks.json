{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "bash|command|execute|run|powershell",
        "hooks": [
          {
            "type": "command",
            "command": "python scripts/session_harvester.py --mode stop"
          }
        ]
      }
    ]
  }
}
